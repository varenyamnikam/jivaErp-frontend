import React, { useEffect, useState, useRef } from "react";

export const Handlesubmenuchange = (setHello, hello) => {
  console.log(hello);
  function handleSubNavChange(setHello, hello) {}
  return handleSubNavChange;
};
